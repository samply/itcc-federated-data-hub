apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itcc-data-lake.fullname" . }}-test-connection"
  labels:
    {{- include "itcc-data-lake.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itcc-data-lake.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
