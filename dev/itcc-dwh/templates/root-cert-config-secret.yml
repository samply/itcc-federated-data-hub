{{- with .Values.config }}
{{- if .rootCrtPem }}
apiVersion: v1
kind: Secret
metadata:
  name: root-cert
  namespace: {{ $.Release.Namespace }}
type: Opaque
stringData:
  root.crt.pem: |-
{{ .rootCrtPem | nindent 4 }}
{{- end }}
{{- end }}